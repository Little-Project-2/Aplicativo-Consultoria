### Result
{
  "treino": "block",
  "dieta": "block",
  "perfil": "block"
}
### Ran Playwright code
```js
await page.evaluate('() => ((() => { switchStudentView(\'treino\'); const treino = getComputedStyle(document.getElementById(\'view-student-treino\')).display; switchStudentView(\'dieta\'); const dieta = getComputedStyle(document.getElementById(\'view-student-dieta\')).display; switchStudentView(\'perfil\'); const perfil = getComputedStyle(document.getElementById(\'view-student-perfil\')).display; return { treino, dieta, perfil }; })())');
```
